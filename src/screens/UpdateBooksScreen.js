import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import UpdateBooks from '../components/account/UpdateBooks'

export default function UpdateBooksScreen() {
  return (
    <View>
      <UpdateBooks/>
    </View>
  )
}

const styles = StyleSheet.create({})